// import { createApp } from 'vue'
// import './style.css'
// import App from './App.vue'

// createApp(App).mount('#app')
/* type MyType = {
  id: string;
  name: string;
  age: number;
};
const user: MyType = {
  id: "1234",
  name: "李哥",
  age: 30,
};
//泛型工具 Partial 将传入的类型转成完全一样,但是所有属性都可选的样子
type MyNewType = Partial<MyType>;
//泛型工具 Readonly 将传入的类型转成完成一样的类型转成完全一样的类型,但是所有属性都是只读
type MyReadOnly = Readonly<MyType>;
//泛型工具 Pick 从第一个泛型中,只选择第二个泛型带的名字属性
type OnlyAge = Pick<MyType, "name">;
//泛型工具 Omit 从第一个泛型中,删除第二个泛型带的名字属性
type OnlyIdName = Omit<MyType, "id">; */
